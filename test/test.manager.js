var domain = require('domain');
var once = require('once');

var manager = require('../lib');

exports.setUp = function(done) {
  done();
}

exports.tearDown = function(done) {
  done();
}

exports.testAddEndpoint = function(test) {
  test.done();
}